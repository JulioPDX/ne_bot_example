# Example Python Bot for NE

1. activate venv: `source ~/.venvs/slackbot/bin/activate`
2. To access the readme, type `netbot help`
3. Get device interfaces, `netbot get network interfaces device=<device name>`
